Check that `$ARGUMENTS/plan.md` exists. If it does not, stop and tell the user to run `/plan $ARGUMENTS` first.

Implement the plan from `$ARGUMENTS/plan.md`.

After completing meaningful work, append a dated entry to `$ARGUMENTS/log.md`:

```
## YYYY-MM-DD
- <what you did>
- <what changed>
- <how you validated it>
```

Stop and ask the user rather than guessing on any decision with significant impact.
